# Copy to .env (gitignored). Never commit real tokens or company hosts.

# Optional: polish heuristic clues. Answers stay fixed.
OPENAI_API_KEY=
TEAM_JEOPARDY_OPENAI_ENABLED=false

# Optional live JIRA import (read-only search). Public samples use jira.example.
JIRA_BASE_URL=
JIRA_EMAIL=
JIRA_API_TOKEN=
JIRA_ACCEPTANCE_FIELD=
JIRA_EPIC_NAME_FIELD=

# Client white-label (copy these into client/.env and rebuild the Vite app).
# VITE_APP_NAME=Team Jeopardy
# VITE_LOGO_URL=
# VITE_ACCENT_COLOR=#f0d060
# VITE_BOARD_COLOR=#0b3d91

