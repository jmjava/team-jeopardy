services:
  server:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    environment:
      TEAM_JEOPARDY_SAMPLE_CODE_PATH: /app/samples/sample-reactor
    volumes:
      - ./samples:/app/samples:ro
